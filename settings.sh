# todo settings
# Copy this file to ~/.config/claude-todo/settings.sh to customize.

# Where todo data is stored (todos.json, notes/)
# TODO_DATA_DIR="$HOME/.claude-todos"

# Default git repo root (auto-detected from cwd if not set)
# TODO_REPO=""

# Editor for notes files (falls back to $VISUAL, then $EDITOR, then open)
# TODO_EDITOR="cursor"
